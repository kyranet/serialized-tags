import serializedTags from './index.js';

export const { ArrayTag } = serializedTags;
export const { BlockTag } = serializedTags;
export const { StringTag } = serializedTags;
export const { ValueTag } = serializedTags;
export const { Util } = serializedTags;
export const { parse } = serializedTags;
